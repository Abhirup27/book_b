{
  "dependencies": {
    "gh-pages": "^6.1.1",
    "three": "^0.160.1"
  },
  "devDependencies": {
    "vite": "^5.0.12"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d ./"
  }
}
